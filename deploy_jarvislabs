cd ~
apt update
apt install software-properties-common screen -y
add-apt-repository ppa:deadsnakes/ppa -y
screen

apt-cache policy python3.8
apt install python3.8 python3.8-distutils python3-dev -y
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
python3.8 get-pip.py
pip install gdown

git clone https://github.com/arkiven4/glow-tts
cd glow-tts
python3.8 -m pip install -r requirements.txt
cd monotonic_align; mkdir monotonic_align; python3.8 setup.py build_ext --inplace
cd ..

gdown https://drive.google.com/uc?id=12TliOfjr9zWiuYiYGVuDxQQiEWL0Azs7
tar -xvzf ToJarvis.tar.gz

# Configure all Setting Like DB, Dkk

TORCH_DISTRIBUTED_DEBUG=DETAIL python train_ms_emo_lang_pitch.py -c configs/base_blank_emo_lang_pitch.json -m base_blank_emo_lang_pitch

tensorboard --logdir=YOUR_LOG_DIR --bind_all
